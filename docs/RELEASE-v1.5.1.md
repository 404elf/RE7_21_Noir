# v1.5.1 修复验证

人机房间从共用固定端口改为独立本机端口，玩家端、AI 端和子进程使用同一端口；只监听回环地址。普通联机端口不变。人机启动重试窗口延长至 15 秒，并检查子进程是否意外退出。

按玩家要求修正加注规则：删除 ADD 分支自带的 give_trump 调用，保留收割的独立奖励。AI 的模拟器复用同一规则，因此不会继续把加注估值为免费补牌。规则 AST 基准更新仅对应这项明确要求的规则修正。

107 项自动测试通过。新增测试覆盖加注 1/2/21 的赌注、手牌数量、无补牌调用，收割的独立奖励，以及固定端口被占用时的两场并发人机和连续重开。

实际 Windows 服务端验证：同样通过端口占用、双人机实例和重开检查；Windows exe 的渲染、双客户端、弃牌、同意平局、再来一局、投降检查通过。

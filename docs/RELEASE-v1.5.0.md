# v1.5.0 验证记录（源码资料）

- 完整自动测试：104 项通过。新增检查覆盖准备时间、四类首动作、HIT/STAY 加秒、无效/迟到操作、每局准备重置、无限时的网络传输、手动查看压过通知队列、相对视角日志、数字牌后的跨决策限制、随机锁定与配置迁移。
- Windows 实际 exe：渲染、两个客户端、弃牌、申请/同意平局、再来一局、投降通过。
- Windows 旧目录迁移实测：先校验备份，迁移自定义生命值和计时；重新启动服务后读到 19 点生命值。首弃牌不增加主时间，STAY 增加秒数，另一方准备超时判负；新根目录保持整洁。
- 计时预设、自定义计时、权重锁定和准备显示进行了无窗口渲染与图片检查。
- 原 GameState 卡牌规则 AST 校验保持通过。数字牌顺序修复按逻辑约束保留，没有以 AI 自对战胜率为理由撤回。
- 玩家压缩包不含测试脚本或测试报告；普通下载包与旧更新器兼容包逐文件校验一致，后者只多一个供旧更新器读取、首次启动即迁移的根目录版本文件。

新计时字段使用协议版本 3。联机双方及房间服务端需要同时升级。
锁定影响本次编辑的一键随机，不影响主动选预设或手动修改。
准备时间只在启用计时时生效，默认每局每人首次行动 30 秒；可独立设为不限时。

复核入口：tests/test_player_release.py、tests/verify_distribution.py、tests/verify_player_package.py。

# 联机使用与部署

## 玩家入口

大厅点击「选择联机方式」。

| 方式 | 用途 | 使用方法 |
| --- | --- | --- |
| 局域网 | 同 Wi-Fi、热点、同一内网 | 房主创建；另一人搜索并点击房间。搜索失败可手动填写地址。 |
| 房间码 | 异地朋友 | 输入房间服务器地址，创建并分享 8 位房间码；可设置密码。双方查看规则、准备后开始。 |
| 地址直连 | 已有可达 IP / 域名 / 端口 | 输入 `192.168.1.10:6666` 或 `[IPv6]:6666`。普通直连限可信网络使用。 |
| 自建服务器 | 固定朋友群、社区 | 收藏自己部署的服务地址，再到房间码页创建/加入。 |

本版没有由作者运营的公共服务器。房间码服务必须先部署，客户端不会默认连接未知第三方服务。服务器负责对局，不是开放代理；不能通过它转发任意地址或访问服务器文件。

房间码连接支持短线重连：服务端保留原座位 30 秒，暂停计时及结算等待；客户端使用仅保存在内存中的随机令牌自动恢复。关闭游戏/主动离开不会在下次启动恢复。普通局域网直连暂不支持重连、密码或准备流程。

这次没有加入自动 P2P 穿透。房间码采用专用服务器方案；地址直连保留可达网络下的点对点连接。没有 UPnP 自动开端口，不修改防火墙，不安装组网驱动。

## 本机验证

Windows 下载包内双击 `start-room-server.cmd`；默认 `room-server.json` 仅监听 `127.0.0.1:7443`。

在游戏房间码页输入 `127.0.0.1:7443`，开两个游戏窗口，一个创建、一个加入并准备。这个默认配置只用于本机验证，异地玩家无法连接。

也可在命令行运行：

```text
RE7_21_Noir.exe --room-server room-server.json
```

源码启动：

```text
python room_server.py --config room-server.json
```

## 公网部署

需要一台玩家可访问的服务器、域名，以及该域名的可信 TLS 证书。运行账号无需管理员权限。证书私钥由部署者在服务器上配置，不发送给玩家，不放入 GitHub 或发布包。

修改服务器本地 `room-server.json`：

```json
{
  "bind": "0.0.0.0",
  "port": 7443,
  "certificate": "certs/fullchain.pem",
  "private_key": "certs/privkey.pem"
}
```

程序直接终止 TLS。没有证书时拒绝公网监听。玩家填写 `tls://你的域名:7443`；证书链、有效期或域名校验失败时会拒绝连接，不降级成明文。证书续期后重启房间服务。

部署者按需允许服务器 TCP 7443 入站。程序不会代替部署者更改系统网络设置。局域网发现使用 UDP 16667，普通直连使用 TCP 6666；不需要为公网房间开放这两个端口。

Linux 可使用 Python 3.12 或更新受维护版本安装 `requirements.txt` 后运行源码服务。建议使用专用低权限账号和进程管理器自动重启。持续维护操作系统、Python、OpenSSL 和证书，外围连接/流量防护仍由部署者负责。

默认最多 32 个房间、96 个已进入协议处理的连接；每个来源 IP 每分钟最多 30 次新连接，每连接每秒最多 30 条请求。等待房间 5 分钟过期，单房间最长 2 小时。额度目前在 `room_server.py` 中设置，共用出口的大型社群需由维护者评估后调整。

## 兼容与故障排查

- v1.4.0 使用安全 JSON 协议，不兼容旧版 pickle 联机。双方及房间服务器必须更新。
- 房间加入失败：检查地址、TLS 证书、房间码、密码及房间是否已满或开始。
- 未发现局域网房间：部分校园网、访客 Wi-Fi 会隔离设备；尝试地址直连或使用房间服务器。
- 公网无法连接：确认域名解析、服务器端口、证书、服务进程；不要关闭整个防火墙来排障。
- 房间服务器的规则与计时统一生效，玩家本地配置不会覆盖已加入的房间。

地址和收藏保存在 `network.json`，密码、重连令牌不会写入该文件。手动更新会保留网络配置和服务器配置。部署证书不在更新包中，保持证书路径可用。

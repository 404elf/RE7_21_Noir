# 网络安全检查与修复（v1.4.0）

## 范围

检查客户端、AI 客户端、局域网服务端、房间服务端、发现协议和手动更新入口。重点是防止远程代码执行、任意文件访问/写入及资源滥用，不以防作弊为目标。这里记录实际修复和验证，不承诺软件不存在未知漏洞。

## 已修复

1. **高危：来自房主的 pickle 数据可执行本机代码。** 客户端及服务端网络收发全部替换为版本化 JSON。原文件中的网络辅助函数也已替换，不保留旧协议降级。GameState 的卡牌规则类保持原样，由 AST 摘要回归测试确认。
2. **不受限报文与积压。** 校验魔数、版本、报文长度、结构深度、数量、数据类型、数值范围和重复键。状态字段白名单禁止覆盖类、方法或任意属性。客户端合并状态快照；服务端限制命令大小、频率、慢速不完整报文、连接与房间数量。
3. **公网认证与传输。** 房间服务要求 TLS；客户端使用系统信任库校验域名和证书，最低 TLS 1.2，不提供忽略验证开关。房间可设密码，恢复座位使用独立随机令牌。仅回环地址允许明文房间服务测试。
4. **不必要的网络暴露。** 人机服务器只绑定回环接口。局域网发现不执行命令、不返回路径、不提供端口映射；以来源地址和随机查询标识匹配响应，并限速。
5. **更新入口复核。** 保留 HTTPS GitHub 来源检查、SHA-256 校验、大小和解压总量限制，阻止目录穿越、符号链接、Windows 设备名、重复路径及覆盖旧安装。服务器消息不能触发更新、修改更新来源或传入可执行文件路径。

网络消息不能指示客户端读取本地文件、运行命令、修改注册表/防火墙、安装驱动或更改音效文件。房间服务不提供任意目标转发功能。

## 验证

- 恶意 pickle 测试载荷只尝试在临时测试目录创建标记文件；新版解析拒绝载荷，标记文件没有生成。
- 超大长度、过深 JSON、重复键、非法版本、非有限数值、方法覆盖、畸形状态均被拒绝。
- 两个房间的配置隔离、错误密码、恶意客户端隔离、准备流程、真实客户端自动重连及暂停计时有回归测试。
- 更新校验失败、恶意压缩包路径、配置备份与自定义预设保留继续测试。
- `pip-audit` 对锁定的运行时 Python 包 `pygame-ce==2.5.8` 查询已知漏洞：未发现已知漏洞。结果见 `docs/dependency-audit-v140.json`。该检查不覆盖系统 Python、OpenSSL、SDL 内嵌组件、操作系统或未知漏洞。

运行测试：

```text
.venv/Scripts/python.exe -m unittest discover -s tests -p "test_*.py"
```

## 边界与维护

普通 TCP 直连没有加密和对端身份验证，只建议用于可信局域网；互联网使用 TLS 房间。应用限流不能阻挡所有分布式拒绝服务，部署者仍应维护服务器及外围防护。GitHub 发布账号和本机环境的安全仍影响更新可信度，当前校验不是独立发布者数字签名。

目前未部署/探测第三方公网服务器。测试针对自有本机服务，不涉及未授权目标。正式公网部署后还需在真实网络下验证证书、可达性和运维配置。

反馈安全问题时请勿公开附带房间密码、重连令牌、TLS 私钥或个人路径信息；提供版本、复现步骤和脱敏错误信息即可。
